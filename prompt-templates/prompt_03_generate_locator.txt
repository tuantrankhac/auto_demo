Generate locator cho module {ModuleName}.

Yêu cầu:
- Đọc practices/browsers/browser.md trước (browser + URL mặc định).
- Nếu {URL} được cung cấp → dùng {URL}; không thì dùng URL trong browser.md.
- Sử dụng Browser MCP để mở đúng trình duyệt / URL theo practice.
- Đọc DOM hiện tại.
- Sinh locator theo Coding Convention.
- Ưu tiên id → name → css → xpath.
- Chỉ sinh locator cho các control thuộc màn hình / form liên quan.
- Kiểm tra PageUI đã tồn tại hoặc tên tương đồng (vd: LoginPageUI ↔ DangNhapPageUI).
  - Giống nhau → mở rộng file hiện có.
  - Không giống → tạo mới.
- Cập nhật / tạo {ModuleName}PageUI.java trong demo.web.pageUIs.
- Không sinh PageObject / Test Script trong bước này.
